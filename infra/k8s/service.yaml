apiVersion: v1
kind: Service
metadata:
  name: account-health-service
  namespace: default
spec:
  type: LoadBalancer
  selector:
    app: account-health-service
  ports:
    - port: 80
      targetPort: 8000